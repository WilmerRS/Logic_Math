
package Unipamplona.MDiscretas.mundo;

import java.util.ArrayList;


public class Conjuntos{
    private ArrayList<Conjunto> conjuntos;

    public ArrayList<Conjunto> getConjuntos() {
        return conjuntos;
    }

    public static void setConjuntos(ArrayList<Conjunto> conjuntos) {
        conjuntos = conjuntos;
    }
    
    public static void modificarConjunto(Conjunto conjunto, int posicion) {
//        conjuntos.remove(posicion);
//        conjuntos.add(posicion, conjunto);
    }
    
    public static void buscarConjunto(String nombre) {
//        conjuntos.add(conjunto);
    }
    
     public static void addConjunto(Conjunto conjunto) {
//        conjuntos.add(conjunto);
    }
}
